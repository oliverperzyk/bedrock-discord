import { expect, test } from "bun:test"

/**
 * @summary Example test.
 * @description This test is used to test the example functionality.
 * @remarks Delete this test after you have added your own tests.
 */
test("Example test.", async (): Promise<void> => {
    expect(2 + 2).toBe(4)
})
