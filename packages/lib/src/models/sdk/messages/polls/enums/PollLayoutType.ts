/**
 * @summary Discord poll layout type.
 * @description Visual layout for a poll. Currently only the default layout exists.
 */
enum PollLayoutType {
    /**
     * @summary Default layout.
     * @description The only layout Discord supports today.
     */
    DEFAULT = 1,
}

export { PollLayoutType }
